import React from 'react';
import Cart from '../Cart/Cart';

const Orders = () => {
    return (
        <div>
            <h2>this is order page</h2>
            <Cart cart={[]}></Cart>
        </div>
    );
};

export default Orders;